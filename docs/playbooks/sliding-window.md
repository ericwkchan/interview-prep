# Sliding Window Playbook

Try it when:
- contiguous subarray/substring
- constraints like "at most k" or "sum/window length"
Checklist:
- expand R each step
- shrink L while constraint violated
- update answer per step
